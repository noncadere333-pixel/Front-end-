## Использование AI-инструментов
В процессе выполнения лабораторной работы использовался AI-инструмент ChatGPT.

## Как использовался AI:

- для объяснения принципов работы хуков useState и useEffect

- для помощи в понимании механизма очистки (cleanup) и массива зависимостей (dependency array)

# Что было сделано самостоятельно:

- все React-компоненты были написаны вручную

- логика приложения была проверена локально путём запуска проекта

- cтруктура проекта была создана самостоятельно

## Чему я научился:

- управлять несколькими состояниями в компоненте

- использовать композицию компонентов и передавать данные через props

- загружать данные с API с помощью useEffect

- использовать функцию очистки (cleanup) для предотвращения утечек памяти